# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# unrelated reference export (nested git repo; see SOURCE.md)
LOVABLE_BUILD/

# build caches (FaCT CSV, postcodes.io overlays, etc.)
.cache/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# UK verifier runtime artifacts (reports under reports/run-* are local; committed samples are named PHASE1_*)
data/verification/state.json
data/verification/audit.jsonl
data/verification/overlay.json
data/verification/latest.json
data/verification/reports/run-*

# US verifier runtime artifacts
data/verification/us/state.json
data/verification/us/audit.jsonl
data/verification/us/overlay.json
data/verification/us/latest.json
data/verification/us/reports/run-*

# vercel

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
